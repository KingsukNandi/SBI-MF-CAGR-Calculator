# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependencies
node_modules

# Next.js
.next
out
next-env.d.ts

# Vercel
.vercel

# Env, matches .env, .env.local, .env.production etc.
.env
.env.*
# ...but the committed template must stay trackable. Without this, .env.*
# matches it, and it only survives today because it is already tracked.
!.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated for AI agents by `next build`
AGENTS.md
CLAUDE.md

# Local working notes and design docs, kept out of the repo
docs
.remember
